import { TimelineBossEvent, TimelinePlayerEvent } from "../types";

export const zoneID = 999;
export const zoneName = "P8Sp1";

export const BossEvents: TimelineBossEvent[] = [];

export const SCHEvents: TimelinePlayerEvent[] = [];
